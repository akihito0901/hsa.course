# HSA教材・学習画面の改訂記録

改訂日：2026年9月14日

## 教材

全7コース・71教材を増補しました。基礎11教材、画像制作・Web制作・SNS・ライティング・AI・営業販売は各10教材です。

改訂前の本文94,501文字に対し、改訂後の記事領域は192,838文字で約2.04倍です。HTMLタグを除いた記事領域を集計しています。改訂後には学習目標、操作ラベル、ワークシート、参照先などの共通文言も含むため、純粋な解説本文だけの増加率ではありません。教材別の値は `content-stats.json` にあります。

各章に固有の追加解説、4段階の実践手順、具体例、コピーできる記入用テンプレート、実践課題、3項目のチェック、確認問題と解説を追加しました。基礎解説も読み直し、パスワード・共有設定・素材の権利・AIの料金や精度・SNSの表示・SEO効果などの断定や古くなりやすい説明を修正しました。実例の数字や案件は練習用として明示しています。

## 画面と操作

- 深緑とアイボリーを中心に、トップ・教材・ログインの画面を刷新。
- 全コースの検索、コース選択、未学習・学習済み・保存済み・無料教材の絞り込み。
- 続きから再開、学習進捗、ブックマーク、前後の教材への移動。
- モバイルの開閉メニュー、折りたたみ目次、文字拡大、読み進めた位置の表示。
- 学習メモの自動保存とTXT書き出し、ワークシートのコピー。
- キーボード操作、フォーカス復帰、動きを減らす設定、印刷用表示。
- 保存領域や外部SDKが利用できない場合の案内。

生成画像は使用せず、画面に合わせたベクター図案を使用しています。動画ファイルはまだ登録していません。

## 検証

- `npm run check`：トップと全71教材の見出し、ID、リンク、教材構造、文字化け、JavaScript構文などを検査。結果は `structural-check.json`。
- `scripts/browser-check.mjs`：393項目合格。全71教材を390px幅で開き、本文・目次・課題・横はみ出しを確認。代表画面は320px幅とデスクトップでも確認。
- `scripts/final-check.mjs`：13項目合格。ログイン後の戻り先、外部リダイレクト防止、モバイルメニューのフォーカス、再開リンク、保存不可時の案内、SDK読込失敗時のログインを確認。
- スクリーンショットは `screenshots/`。ブラウザ検証ではページ内JavaScript例外なし。

ブラウザ検証にはPlaywrightを使用しました。ローカルにインストールされた `playwright-core` のパスを `HSA_PLAYWRIGHT` 環境変数に設定すると検証スクリプトを実行できます。認証状態はテスト用の応答で再現しています。実際の会員登録、メール送信、Google認証、Stripe決済、Webhook、本番環境の動作は今回実施していません。

## 運用の前提

進捗・メモ・ブックマークはブラウザ内保存で、端末間・アカウント間の同期はありません。同じブラウザを使う人同士では保存内容を共有します。メモはTXTでも控えを残せます。

無料・購入済み・管理者の画面表示は既存の認証構成を引き継いでいます。教材本文は静的HTMLに含まれており、既存のクライアント側ガードは本文ファイル自体へのアクセスを保護する仕組みではありません。サーバー側での有料本文配信制御は今回の変更範囲に含みません。既存の `SETUP.md` も参照してください。

今回の成果物はローカルの変更です。公開・デプロイ・コミット・プッシュは行っていません。

## 公開前の追加確認

2026年9月14日、既存のGitHubリポジトリ `akihito0901/hsa.course` の `main` とVercelのProduction連携を確認しました。既存本番は https://hsa-course.vercel.app です。

`vercel.json` で公開用静的出力を `public` に指定し、`api/*.js` をFunctionとして明示しました。ステージ処理は決済・Webhook・管理者APIの3ファイルの存在と構文も検査します。APIは公開用静的フォルダへコピーせず、ルートの `api` に保持しています。ただし、Vercel上の実ビルドで3本が生成されることは、まだ確認していません。

会員情報の取得・更新・プロフィール確認に10秒の待ち時間制限を追加しました。認証障害時に再購入を促さず、無料教材は利用できます。`auth-timeout-check.json` の5項目で確認済みです。

本番画面は「会員状態を確認しています…」のままになっており、ログイン済み状態は未確認です。ブラウザの既存タブにSupabase一時停止の通知タイトルがありましたが、管理画面の現在の状態は未確認です。

自動承認レビューにより、本番へのコミット・プッシュとSupabase管理画面へのアクセスが拒否されました。前者はAPI欠落リスクと本番反映承認、後者は別サービスへのアクセス承認が理由です。外部の変更は実施していません。管理画面の確認・必要な再開、VercelビルドのAPI確認、本番反映と実環境検証が残っています。
